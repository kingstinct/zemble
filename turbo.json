{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {
      "outputMode": "errors-only",
      "inputs": [ "**/*" ]
    },
    "lint-quiet": {
      "outputMode": "errors-only",
      "inputs": [ "**/*" ]
    },
    "test": {
      "outputMode": "errors-only",
      "inputs": [
        "**/*.ts",
        "**/tsconfig.json"
      ]
    },
    "build": {
      "outputMode": "errors-only",
      "inputs": [
        "**/*.ts",
        "**/tsconfig.json"
      ],
      "outputs": [
        "*.js",
        "*.d.ts"
      ]
    },
    "clean":{
      "outputMode": "errors-only",
      "cache": false
    },
    "typecheck": {
      "outputMode": "errors-only",
      "inputs": [
        "**/*.ts",
        "**/*.tsx",
        "**/*.js",
        "**/*.jsx"
      ]
    },
    "graphql-codegen": {
      "outputMode": "errors-only",
      "inputs": [
        "**/*.graphql",
        "**/codegen.ts"
      ]
    }
  }
}
