{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {
      "dependsOn": [ "codegen" ],
      "outputMode": "errors-only"
    },
    "test": {
      "dependsOn": [ "codegen" ],
      "outputMode": "errors-only",
      "inputs": [
        "**/*.ts",
        "**/*.tsx",
        "**/*.js",
        "**/*.jsx",
        "**/tsconfig.json",
        "**/package.json"
      ]
    },
    "typecheck": {
      "dependsOn": [ "codegen" ],
      "outputMode": "errors-only",
      "inputs": [
        "**/*.ts",
        "**/*.tsx",
        "**/*.js",
        "**/*.jsx",
        "**/tsconfig.json",
        "**/package.json"
      ]
    },
    "codegen": {
      "dependsOn": [ "^codegen" ],
      "outputMode": "errors-only",
      "inputs": [
        "**/*.graphql",
        "**/codegen.ts",
        "**/package.json"
      ]
    }
  }
}
