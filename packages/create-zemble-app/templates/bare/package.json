{
  "name": "pkgname",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "@zemble/core": "latest",
    "@zemble/bun": "latest"
  },
  "scripts": {
    "start": "bun serve.ts",
    "test": "bun test",
    "dev": "bun --hot serve.ts",
    "typecheck": "tsc --noEmit",
    "build": "(rm index.d.ts || true) && zshy"
  },
  "devDependencies": {
    "@tsconfig/bun": "^1.0.1"
  },
  "peerDependencies": {},
  "module": "serve.ts",
  "main": "serve.ts",
  "zshy": {
    "exports": {
      "./app.test-template": "./app.test-template.ts",
      "./config": "./config.ts",
      "./serve": "./serve.ts"
    }
  },
  "exclude": [
    "**/*.ts",
    "**/*.tsx",
    "**/*.cts",
    "**/*.mts",
    "node_modules"
  ],
  "files": [
    "*"
  ]
}
