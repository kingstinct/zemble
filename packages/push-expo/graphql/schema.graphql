enum ExpoPushPlatform {
  android
  ios
  web
}

type Mutation {
  registerExpoPushToken(pushToken: String!, platform: ExpoPushPlatform!): Boolean!
  sendPushNotification(pushToken: String!, title: String!, body: String!): Boolean!
}
