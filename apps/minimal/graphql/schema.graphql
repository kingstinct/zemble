type Query {
  hello: String!
}

type Mutation {
  randomNumber: Int!
  # sendSms(
  #   from: String!,
  #   to: String!,
  #   message: String!,
  # ): Boolean!
}

type Subscription {
  countdown(from: Int!): Int!
  tick: Float!
  randomNumber: Int!
}
