directive @skipAuth on FIELD_DEFINITION

type Mutation {
  login(username: String!): LoginResponse! @skipAuth
}

type LoginResponse {
  token: String! @skipAuth
}

type User {
  userId: ID!
  username: String!
}

type Query {
  me: User!
}
