scalar JSON

type Mutation {
  generateMaintenanceKey(maintenanceSecret: String!, expiresInSeconds: Int): SignResponse!
}

type SignResponse {
  token: String!
}

type Query {
  validate(token: String!): Boolean!
  read(token: String!): JSON!
  publicKey: String!
}
